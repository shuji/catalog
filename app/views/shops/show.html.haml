%p#notice= notice

%p.name
  %b Name:
  = @shop.name
.description
  %p
    %b Description:
    = simple_format @shop.description
.lines_summary
  %p
    %b Lines summary:
    = simple_format @shop.lines_summary
%p.image
  = image_tag(@shop.image_url) if @shop.image?

= link_to 'Edit', edit_shop_path(@shop)
\|
= link_to 'Back', shops_path
